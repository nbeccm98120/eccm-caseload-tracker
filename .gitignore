# ── Electron app specific ─────────────────────────────────────
# Build output
dist/
out/

# User data — never commit real SC data or credentials
caseload_data.json
settings.json
pending_azure_upload.json
*.json.bak

# Azure credentials — lives on each machine, never in git
config.json

# Dev-only update config — version-specific, not for repo
dev-app-update.yml

# ── Node.js standard ──────────────────────────────────────────
node_modules/
jspm_packages/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage
coverage
*.lcov
.nyc_output

# Caches
.cache
.parcel-cache
.npm
.eslintcache
.stylelintcache

# Build artifacts
build/Release
*.tsbuildinfo
*.tgz

# dotenv
.env
.env.*
!.env.example

# ── OS ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ───────────────────────────────────────────────────────
.vscode/
.idea/
*.suo
*.user
